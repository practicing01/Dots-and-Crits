function DotsandCritswindow::onTouchDown(%this,%touchID,%worldPosition,%mouseClicks)
{
//echo("DotsandCritswindow touch down" SPC DotsandCritswindow.getMousePosition() SPC %worldPosition);
return;
}

function scenewindow_player1::onTouchDown(%this,%touchID,%worldPosition,%mouseClicks)
{
//echo("scenewindow_player1 touch down" SPC scenewindow_player1.getMousePosition() SPC %worldPosition);
return;
}

function scenewindow_player2::onTouchDown(%this,%touchID,%worldPosition,%mouseClicks)
{
//echo("scenewindow_player2 touch down" SPC scenewindow_player2.getMousePosition() SPC 
//$joyobject.cursorgui.Position SPC %worldPosition);
return;
}
